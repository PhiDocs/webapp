{
  "entities": {
    "user": {
      "title": "User",
      "description": "Represents a user in the system.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The user's unique ID from Firebase Auth." },
        "name": { "type": "string", "description": "The user's full name." },
        "email": { "type": "string", "format": "email", "description": "The user's email address." },
        "role": { "type": "string", "enum": ["admin", "user"], "description": "The user's role, as stored in Firestore. The source of truth for authorization is the custom claim." },
        "companyId": { "type": "string", "description": "ID of the company the user belongs to." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of creation." }
      },
      "required": ["uid", "name", "email", "role", "createdAt"]
    },
    "company": {
      "title": "Company",
      "description": "Represents a company using the system.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the company." },
        "ownerUid": { "type": "string", "description": "The UID of the admin user who owns the company." }
      },
      "required": ["name", "ownerUid"]
    },
    "work": {
      "title": "Work",
      "description": "Represents a specific job or construction site.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the work/site." },
        "address": { "type": "string", "description": "The address of the work/site." },
        "companyId": { "type": "string", "description": "The ID of the company this work belongs to." },
        "startDate": { "type": "string", "format": "date", "description": "The start date of the work." },
        "endDate": { "type": "string", "format": "date", "description": "The end date of the work." },
        "workLocationDetails": { "type": "string", "description": "Details about the specific location or floor of the work." }
      },
      "required": ["name", "companyId", "startDate", "endDate", "workLocationDetails"]
    },
    "employee": {
      "title": "Employee",
      "description": "Represents an employee of a company.",
      "type": "object",
      "properties": {
        "firstName": { "type": "string", "description": "Employee's first name." },
        "lastName": { "type": "string", "description": "Employee's last name." },
        "role": { "type": "string", "description": "Employee's job title or function." },
        "company": { "type": "string", "description": "Name of the subcontracting company, if applicable." },
        "email": { "type": "string", "format": "email", "description": "Employee's email address." },
        "cpf": { "type": "string", "description": "Employee's CPF (Brazilian individual taxpayer registry)." },
        "companyId": { "type": "string", "description": "The ID of the company this employee belongs to." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of creation." }
      },
      "required": ["firstName", "lastName", "role", "email", "cpf", "companyId", "createdAt"]
    },
    "jobRole": {
      "title": "Job Role",
      "description": "Represents a job role within a company.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "Name of the job role (e.g., Eletricista, Soldador)." },
        "responsibilities": { "type": "string", "description": "A summary of the role's responsibilities." },
        "requiredCertificates": { "type": "array", "items": { "type": "string" }, "description": "List of required certificates (e.g., NR-10, NR-35)." },
        "companyId": { "type": "string", "description": "The ID of the company this role belongs to." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of creation." }
      },
      "required": ["name", "responsibilities", "companyId", "createdAt"]
    },
    "subcontractor": {
      "title": "Subcontractor",
      "description": "Represents a third-party company.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "Name of the subcontracting company." },
        "cnpj": { "type": "string", "description": "CNPJ of the company." },
        "contractNumber": { "type": "string", "description": "The contract number with the main company." },
        "companyId": { "type": "string", "description": "The ID of the main company this subcontractor is associated with." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of creation." }
      },
      "required": ["name", "cnpj", "companyId", "createdAt"]
    },
    "document": {
      "title": "Safety Document",
      "description": "Represents a generated safety document (APR or PT).",
      "type": "object",
      "properties": {
        "workId": { "type": "string", "description": "The ID of the work this document is for." },
        "type": { "type": "string", "enum": ["APR", "PT"], "description": "The type of document." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of creation." },
        "formData": { "type": "object", "description": "The raw form data used to generate the document." },
        "analysisData": { "type": "object", "description": "The AI-generated analysis data for APRs." },
        "equipmentData": { "type": "object", "description": "The AI-generated equipment recommendations for APRs." }
      },
      "required": ["workId", "type", "createdAt", "formData"]
    },
    "errorLog": {
        "title": "Error Log",
        "description": "Represents a log of an error that occurred in the application for debugging purposes.",
        "type": "object",
        "properties": {
            "timestamp": { "type": "string", "format": "date-time", "description": "When the error occurred." },
            "functionName": { "type": "string", "description": "The name of the function where the error was caught (e.g., 'signUp', 'signIn')." },
            "userEmail": { "type": "string", "description": "The email of the user involved, if available." },
            "errorCode": { "type": "string", "description": "The specific error code from Firebase or other services." },
            "errorMessage": { "type": "string", "description": "The detailed error message." },
            "stackTrace": { "type": "string", "description": "The stack trace of the error, if available." }
        },
        "required": ["timestamp", "functionName", "errorMessage"]
    }
  },
  "auth": {
    "providers": ["email"],
    "customClaims": {
        "role": {
            "description": "Defines the user's role in the system.",
            "type": "string",
            "values": {
                "admin": "Grants access to administrative routes and functionalities.",
                "user": "Default role for standard users with access to document generation."
            }
        },
        "companyId": {
            "description": "Associates a user with a specific company.",
            "type": "string"
        }
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/user" },
      "description": "Stores public profiles for users."
    },
    "/companies/{companyId}": {
      "schema": { "$ref": "#/entities/company" },
      "description": "Stores information about each company."
    },
    "/companies/{companyId}/employees/{employeeId}": {
      "schema": { "$ref": "#/entities/employee" },
      "description": "Stores employees for a specific company."
    },
    "/companies/{companyId}/jobRoles/{jobRoleId}": {
      "schema": { "$ref": "#/entities/jobRole" },
      "description": "Stores job roles for a specific company."
    },
    "/companies/{companyId}/subcontractors/{subcontractorId}": {
      "schema": { "$ref": "#/entities/subcontractor" },
      "description": "Stores subcontractors for a specific company."
    },
    "/works/{workId}": {
      "schema": { "$ref": "#/entities/work" },
      "description": "Stores information about each work site, belonging to a company."
    },
    "/documents/{documentId}": {
      "schema": { "$ref": "#/entities/document" },
      "description": "Stores all generated safety documents."
    },
    "/errorLogs/{logId}": {
        "schema": { "$ref": "#/entities/errorLog" },
        "description": "Stores application errors for debugging."
    }
  }
}
